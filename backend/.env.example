# The port the backend server listens on during development and production.
PORT=3000

# The runtime environment used by the application to control behavior such as logging and error handling.
NODE_ENV=development

# The MongoDB connection string used by the backend when database integration is added.
MONGO_URI=mongodb+srv://<db_username>:<db_password>@smart-hydroponics.immdan9.mongodb.net/
